# Built-in Styles

Add bundled style reference images as `.png` or `.jpg` files in this folder.
The application automatically lists them in the `Примеры` dialog.

`impressionist_texture.png` is included as the default educational style input.
