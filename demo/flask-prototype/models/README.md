# TensorFlow Lite Models

Place the two compatible models in this directory:

```text
style_predict.tflite
style_transform.tflite
```

Do not create empty placeholder `.tflite` files: without actual model binaries
the web interface intentionally reports a clear missing-model message.
